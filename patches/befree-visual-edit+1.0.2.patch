diff --git a/node_modules/befree-visual-edit/dist/babel.js b/node_modules/befree-visual-edit/dist/babel.js
index 192da9d..d354a2b 100644
--- a/node_modules/befree-visual-edit/dist/babel.js
+++ b/node_modules/befree-visual-edit/dist/babel.js
@@ -227,10 +227,29 @@ function shouldSkipOid(name) {
   const leafName = fullName.split(".").pop() ?? fullName;
   return UNSAFE_ARBITRARY_PROP_COMPONENT_FULL_NAMES.has(fullName) || UNSAFE_ARBITRARY_PROP_COMPONENTS.has(leafName);
 }
-function randomOid() {
+const cyrb53 = (str, seed = 0) => {
+  let h1 = 0xdeadbeef ^ seed, h2 = 0x41c6ce57 ^ seed;
+  for (let i = 0, ch; i < str.length; i++) {
+    ch = str.charCodeAt(i);
+    h1 = Math.imul(h1 ^ ch, 2654435761);
+    h2 = Math.imul(h2 ^ ch, 1597334677);
+  }
+  h1 = Math.imul(h1 ^ (h1 >>> 16), 2246822507);
+  h1 ^= Math.imul(h2 ^ (h2 >>> 13), 3266489909);
+  h2 = Math.imul(h2 ^ (h2 >>> 16), 2246822507);
+  h2 ^= Math.imul(h1 ^ (h1 >>> 13), 3266489909);
+  return 4294967296 * (2097151 & h2) + (h1 >>> 0);
+};
+function getDeterministicOid(filename, loc, index) {
+  const normalized = filename.replace(/\\/g, "/").replace(/^.*\/src\//, "src/");
+  const seed = `${normalized}::${loc}::${index}`;
+  const hash = cyrb53(seed);
+  const chars = "abcdefghijklmnopqrstuvwxyz0123456789";
   let id = "";
-  for (let i = 0;i < ID_LEN; i++) {
-    id += VALID_CHARS[Math.floor(Math.random() * VALID_CHARS.length)];
+  let n = hash;
+  for (let i = 0; i < 7; i++) {
+    id += chars[n % chars.length];
+    n = Math.floor(n / chars.length);
   }
   return id;
 }
@@ -238,6 +257,9 @@ function visualEditBabelPlugin() {
   return {
     name: "visual-edit-inject-oids",
     visitor: {
+      Program(path) {
+        this.jsxCounter = 0;
+      },
       JSXOpeningElement(path) {
         const { node } = path;
         const name = node.name;
@@ -252,14 +274,18 @@ function visualEditBabelPlugin() {
         if (hasOid)
           return;
         const t = path.hub.file.opts.parserOpts?.plugins ? __require("@babel/types") : path.scope.hub.file.opts.babelrc !== undefined ? __require("@babel/types") : { jSXAttribute: null };
+        const filename = this.file?.opts?.filename || path.hub?.file?.opts?.filename || "";
+        const loc = node.loc ? `${node.loc.start.line}:${node.loc.start.column}` : "";
+        this.jsxCounter = (this.jsxCounter || 0) + 1;
+        const oid = getDeterministicOid(filename, loc, this.jsxCounter);
         try {
           const babel_t = __require("@babel/types");
-          node.attributes.push(babel_t.jSXAttribute(babel_t.jSXIdentifier(DATA_OID_ATTR), babel_t.stringLiteral(randomOid())));
+          node.attributes.push(babel_t.jSXAttribute(babel_t.jSXIdentifier(DATA_OID_ATTR), babel_t.stringLiteral(oid)));
         } catch {
           node.attributes.push({
             type: "JSXAttribute",
             name: { type: "JSXIdentifier", name: DATA_OID_ATTR },
-            value: { type: "StringLiteral", value: randomOid() }
+            value: { type: "StringLiteral", value: oid }
           });
         }
       }
diff --git a/node_modules/befree-visual-edit/dist/cli.js b/node_modules/befree-visual-edit/dist/cli.js
index 6efac9b..81b63b1 100755
--- a/node_modules/befree-visual-edit/dist/cli.js
+++ b/node_modules/befree-visual-edit/dist/cli.js
@@ -48826,10 +48826,32 @@ function replaceNodeClasses(node, className) {
 function classNameAttr(opening) {
   return opening.attributes.find((attr) => t.isJSXAttribute(attr) && t.isJSXIdentifier(attr.name) && attr.name.name === "className");
 }
+function hasIdentifier(node, name) {
+  if (!node)
+    return false;
+  if (t.isIdentifier(node) && node.name === name)
+    return true;
+  if (t.isMemberExpression(node) && t.isIdentifier(node.property) && node.property.name === name)
+    return true;
+  if (t.isLogicalExpression(node)) {
+    return hasIdentifier(node.left, name) || hasIdentifier(node.right, name);
+  }
+  if (t.isJSXExpressionContainer(node)) {
+    return hasIdentifier(node.expression, name);
+  }
+  return false;
+}
 function setClassNameToSlotProp(node, propExpression) {
   const attr = classNameAttr(node.openingElement);
+  if (attr && attr.value && t.isJSXExpressionContainer(attr.value)) {
+    const propName = t.isIdentifier(propExpression) ? propExpression.name : 
+                     t.isMemberExpression(propExpression) && t.isIdentifier(propExpression.property) ? propExpression.property.name : "";
+    if (propName && hasIdentifier(attr.value.expression, propName)) {
+      return;
+    }
+  }
   const fallback = attr?.value ? t.isStringLiteral(attr.value) ? t.stringLiteral(attr.value.value) : t.isJSXExpressionContainer(attr.value) && !t.isJSXEmptyExpression(attr.value.expression) ? attr.value.expression : t.stringLiteral("") : t.stringLiteral("");
-  const value = t.jsxExpressionContainer(t.logicalExpression("||", propExpression, fallback));
+  const value = t.jsxExpressionContainer(t.logicalExpression("??", propExpression, fallback));
   if (attr)
     attr.value = value;
   else
